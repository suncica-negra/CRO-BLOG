<%- include("partials/header2"); -%>

  <h1><%=title%></h1>
  <p><%=content%></p>
  <p class="pull-right"><em><%=date%></em></p>

<p class="potpis">Vaša CRO シ 😘</p>

<div class="komentar-polje">
<p><a type="submit" href="/posts/<%= requestedPostId %>/comment" class="btn btn-primary">Komentiraj</a></p>
</div>

<div class="komentari">
  <%  comments.forEach(function(comment){ %>

<strong class="autor"><%= comment.author %></strong> kaže: <em><%= comment.content %></em>
<p><small><em><%= comment.date.toDateString() %></em></small></p>

	<% if( korisnik == comment.author ){ %>

<form id="myDIV" class="" method="POST">
<a type="button" class="btn btn-xs btn-success" href="/posts/<%= requestedPostId %>/comment/<%= comment._id %>/<%= comment.author %>/edit" >Uredi</a>
<a type="button" class="btn btn-xs btn-danger" href="/posts/<%= requestedPostId %>/comment/<%= comment._id %>/<%= comment.author %>/delete" >Obriši</a>
</form>

	<% } else { %>

<form id="myDIV" class="hide" method="POST">
<a type="button" class="btn btn-xs btn-success" href="/posts/<%= requestedPostId %>/comment/<%= comment._id %>/<%= comment.author %>/edit" >Uredi</a>
<a type="button" class="btn btn-xs btn-danger" href="/posts/<%= requestedPostId %>/comment/<%= comment._id %>/<%= comment.author %>/delete" >Obriši</a>
</form>

	<% } %>


    <% }) %>


</div>


<%- include("partials/footer"); -%>
